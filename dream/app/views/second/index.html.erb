<div id="graph1">
<script type = "text/javascript">

	var dataset = [];
	d3.csv("../../../dream.csv", function(data) {
		dataset = data.map(function (d) {
			return [+d.frequency, +d.memory];
		});
		console.log(dataset);
	});

/*	var w = 500;
	var h = 200;
	var svg = d3.select("body")
	  			.append("svg")
				.attr("width", w);
				.attr("height", h);


	svg.selectAll("circle")
		.data(dataset)
		.enter()
		.append("circle")
		.attr("cx", function(d) { return d[0]; })
		.attr("cy", function(d) { return d[1]; })
		.attr("r", runction(d) { return d[3]; });
*/



</script>
</div>
